[project]
name = "chunk-elicitation-replication"
version = "0.1.0"
description = "Local JSON replication bundle for chunk elicitation simulations."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy",
    "openai",
    "pandas",
    "plotly",
    "python-dotenv",
    "requests",
    "schema>=0.7.8",
    "scipy",
    "seaborn>=0.13.2",
    "statsmodels",
]

