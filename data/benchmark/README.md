# Processed Benchmarks

Files in this folder are generated from `data/raw/` by:

```bash
uv run python scripts/01_Process_Benchmark.py
```

`benchmarks.json` is the local JSON replacement for the benchmark collection
used by the analysis code.
